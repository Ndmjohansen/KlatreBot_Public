CONTEXT (recent chat):
${context}

Asking user Discord ID: ${asking_user_id}

CHANNEL_ID: ${channel_id}

MENTION_TOKENS (use exact token to ping a user):
${mention_tokens}

KNOWN_USER_ALIASES:
${alias_map}
Use people_names in memory tool calls for these aliases.

QUESTION: ${question}
